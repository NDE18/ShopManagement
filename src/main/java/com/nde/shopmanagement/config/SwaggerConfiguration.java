package com.nde.shopmanagement.config;

/*import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import springfox.documentation.builders.ApiInfoBuilder;
import springfox.documentation.builders.PathSelectors;
import springfox.documentation.builders.RequestHandlerSelectors;
import springfox.documentation.spi.DocumentationType;
import springfox.documentation.spring.web.plugins.Docket;
import springfox.documentation.swagger2.annotations.EnableSwagger2;

import static com.nde.shopmanagement.utils.Constants.APP_ROOT;

@EnableSwagger2
public class SwaggerConfiguration {

    @Bean
    public Docket api(){
        return new Docket(DocumentationType.SWAGGER_2)
                .apiInfo(
                        new ApiInfoBuilder()
                        .description("Shop management API documentation")
                        .title("Shop management REST API")
                        .build()
                )
                .groupName("REST API v1")
                .select()
                .apis(RequestHandlerSelectors.basePackage("com.nde.shopmanagement"))
                .paths(PathSelectors.ant(APP_ROOT + "/**"))
                .build();

    }
}*/
